<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>Result</title>
</head>
<body>
    <div class="container justify-content-center mt-5" style="margin-bottom: 80%;">

        <h3 class="text-center">Search Result >> <span class="text-danger"> <%= searchQuery %> </span></h3>
            <div class="container">
                    <% if(data.results.length > 0){ %>
                        <% data["results"].forEach(movie=>{ %>
                        <div class="row">
                            <div class="col-12 col-lg-8">
                                <img src="https://image.tmdb.org/t/p/w600_and_h900_bestv2/<%= movie.poster_path %>" alt="" class="img-fluid" style="max-width: 100%;" height="100px" alt="avenger picture">
                            </div>
                            <div class="col-lg-4">
                                <h4 class="mt-5"><%= movie["title"] %></h4>
                                <hr>
                                <p class="text-mute mt-5"><%=movie["overview"] %></p>
                                <hr>
                                <p class="mt-5"> <strong>Date Published</strong> <%= movie["release_date"] %></p>
                                <p class="mt-5"> <strong>Total Votes:</strong> <%= movie["vote_count"] %></p>
                                <p class="mt-5"> <strong>Averate Rating:</strong> <%= movie["vote_average"] %></p>
                            </div>
                        </div><hr>  
                        <% }) %>
                    <% }else{ %>
                    <h1 class="text-danger text-center">Sorry No Result Found</h1>
                <% } %>
        </div>
    </div>
    <div class="text-center footer">
        <div class="card text-white bg-danger">
            <div class="card-header">
                About
            </div>
            <div class="card-body">
                <div class="card-title">
                    A Movie App
                </div>
                <p class="card-text">
                    This is a simple app built using Nodejs, EJS Template Engine and the Movie DB API
                </p>
                <hr class="bg-white">
                copyright @Theodore Kelechukwu Onyejiaku
            </div>
        </div>
    </div>    
</body>
</html>